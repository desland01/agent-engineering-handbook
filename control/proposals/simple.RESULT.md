# Authoring result — simple, the three prose rules from ARCHITECT.md

Date: 2026-09-10. Owner decision 1, approved: move the rules added to `ARCHITECT.md` today into
the methods workers actually receive.

**Why here.** `ARCHITECT.md` is loaded into every session's system prompt and into no worker's
capsule. A capsule delivers only its named Charts, so a rule written in the contract governs
the parent and not the fleet. All three rules govern prose a human reads, which is `simple`'s
subject, and every writing worker today already required `simple`.

**What is added.** Three sections after the deletion rule: headings state their section's claim;
the instruction is not the output; five seconds from any viewpoint. Nothing existing changes;
frontmatter untouched.

**Evidence each rule carries.** The heading rule cites the measured failure the owner caught
("Repeated fixes spend tokens" read as its own reverse) and the 72-of-76 label rate the earlier
subject-naming instruction produced. The instruction-is-not-the-output rule cites the schema
labels shipped as headings on 19 pages and the stock sentence pasted into ten of eleven changes
during the Chart correction. The five-second rule is the owner's ruling of 2026-09-10.

**Sequencing, which matters.** `ARCHITECT.md` keeps these rules until this proposal is
published. Trimming the contract first would leave a window in which neither the contract nor
any Chart carries them. Publish, verify a worker receives them, then trim.

**Checks.** Instruction-only change: compared against the owner's requirement and the existing
body. No behavioural check applies to the wording; the rules' executable half already lives in
`build/check.py`, `build/check-render.js` and the Chart-correction checker.
