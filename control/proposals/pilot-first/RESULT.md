# Authoring result — pilot-first, standalone

Date: 2026-09-10. This is the standalone Chart, kept ready in case the owner prefers a distinct
skill over the combination folded into `long-running-harness` (that decision needs a Core skill
retired, since Core stands at its cap of 50 — plan.md §6).

**Source.** `SKILL.original.md`, the verbatim body from `~/.agents/skills/pilot-first`, with its
ablation evidence in `eval/`. Body, loop, stop conditions, provenance and measured results are
unchanged.

**What is added**, from the owner's correction of 2026-09-10 ("not to give up after they fail on
the first try... pilot it, fix what didn't work, pilot it again"): two sections — pilot the
machine and not only the change, and a failed pilot is the pilot working — plus the measured
record of the five construction faults that refused a 67-item batch launched without a harness
pilot.

**Unresolved.** The `file:///Users/thebeast/.claude/skills/...` links in the provenance section
no longer resolve; the artifacts are preserved beside this file in `eval/`. Fix the links at
publication or drop them for the `eval/` path.
