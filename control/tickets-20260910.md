# Design-system tickets

## Scope, sources and dispatch contract

This is a backlog, not a dispatch or authorization to execute it. Only this file is delivered. No workers, network calls, publication, commits or runtime changes were performed. All tickets are **NOT READY**: the workspace contains planning inputs, not the editable handbook, skill bodies, runtime source or rendered evidence. Each ticket names its additional blockers. Parent-supplied inputs must be materialized inside the eventual worker workspace, with exact absolute paths, before dispatch; a descriptive supply request below is not a resolved path.

**Existing input manifest.** The following labels expand to these exact absolute paths. Every ticket receives P, S, H, N and R; its Inputs line adds the ticket-specific materials. These are input aliases, not invented files.

- P: `/Users/thebeast/.nautilus/releases/browser-route-20260910/var/workspaces/handbook-20260910-workers/astra-tickets/premium-tech-design-plan.md`
- S: `/Users/thebeast/.nautilus/releases/browser-route-20260910/var/workspaces/handbook-20260910-workers/astra-tickets/design-skill-standard.md`
- H: `/Users/thebeast/.nautilus/releases/browser-route-20260910/var/workspaces/handbook-20260910-workers/astra-tickets/handoff-20260910.md`
- N: `/Users/thebeast/.nautilus/releases/browser-route-20260910/var/workspaces/handbook-20260910-workers/astra-tickets/design-notes.md`
- R comprises all five: `/Users/thebeast/.nautilus/releases/browser-route-20260910/var/workspaces/handbook-20260910-workers/astra-tickets/claude-fable-5-1.route.md`; `/Users/thebeast/.nautilus/releases/browser-route-20260910/var/workspaces/handbook-20260910-workers/astra-tickets/gpt-6-astra.route.md`; `/Users/thebeast/.nautilus/releases/browser-route-20260910/var/workspaces/handbook-20260910-workers/astra-tickets/glm-5.3-flash.route.md`; `/Users/thebeast/.nautilus/releases/browser-route-20260910/var/workspaces/handbook-20260910-workers/astra-tickets/grok-4.5.route.md`; `/Users/thebeast/.nautilus/releases/browser-route-20260910/var/workspaces/handbook-20260910-workers/astra-tickets/ROUTE-FORMAT.md`.

**Declared worker outputs.** `deliverable.patch` and `RESULT.md` are the output convention evidenced in N's Delegation log. Below they are prospective outputs, not files claimed to exist here. Each worker gets its own parent-bound absolute workspace; those names resolve only within that workspace. Reports, plans, measurements, review findings and answers belong in `RESULT.md` unless a source explicitly names another file. Parent must supply exact target paths for every patch; names below identify source-mentioned targets, not verified local files. An unresolved target remains NOT READY. No shared output directory or concurrent writes to one checkout.

**Dispatch requirements for every ticket:** bind the absolute workspace and `releaseDigest` in the same launch step; supply a base resolvable in that checkout (its own `HEAD`, not a parent-repository hash); declare readable files and SHA256 values for GLM; use `python3 -m venv --copies` if a venv is needed. Native Skill remains in the allowed tools. The three selected routes declare null/null route limits with paired null `expiresAt`; the historical 600-second runtime limit is separate, not proof of unlimited execution. Do not change settings to make a ticket fit. Select the exact route once at dispatch. Grok receives no ticket: its descriptor establishes no suitable acceptance history.

**Evidence and gate requirements:** parent supplies renders and browser measurements; the historical workers had no browser. Tiles must be ≤2000px on both axes, retain page/width identity and cover the reviewed area; failed image reads are missing evidence, not passes. Every visual change requires independent rendered review, two reviewers in parallel, then the maker's per-finding applied/overruled-with-reason answer before landing. Astra can review rendered tiles. GLM receives text/source/measurement evidence only: its descriptor explicitly prohibits images and visual verdicts. The plan's GLM composition/taste gate and GLM visual comparison builds therefore remain unresolved, not silently replaced by source review. A supported, authorized resolution must precede those gates. Do not claim two-route visual acceptance from one rendered reviewer plus a text checker.

**Chart delivery:** N's later Fleet correction supersedes the claim that skill-packs lacked Chart files: bundles existed but required-Chart delivery failed at zero requests. That line cannot be dispatched with a required Chart under the recorded workaround. This backlog does not authorize dropping a required Chart to evade a failure; use an authorized working delivery path or leave the ticket blocked. Browser-route/task-file/premium-tech-design completion is historical evidence, not verification of today's launch.

**State precedence:** H's later update reports B1's 76 sections rewritten and second-route reviewed, B2 drawings redrawn but rendered review pending, and scroll capture fixed with a wheel probe. Confirm those artifacts before doing new work; do not repeat the stale “in flight,” “not started” or red-check states. H's “eight product skills installed” and “runtime patch not applied” do not establish governed delivery. Historical credits differ (5,841 in P; 5,847 in H/N); neither is a current balance. Hero copy remains an open B3 decision; frames stay a quiet timestamp strip unless explicitly reversed. SITE_URL, runtime duration changes and the older optional Three.js/mark/share-clip ideas are not added to P's A–D scope.

## Waves (§4, with prerequisites made explicit)

| Wave | Parallel lanes and sequencing |
|---|---|
| 0 — prepare | C1 → C2 gate artifacts; C3 existing-check reconciliation/extension; obtain owner decisions and missing source/evidence. These support landing, not a new approval stage. |
| 1 — now | A1 author → Astra review → A6 per-body publication gate; B1a → B1b ∥ B1c → B1d; B2 inspect existing drawings → rendered review. Separate workspaces; serialize integration of overlapping handbook files. |
| 2 — next | A2 author → review → A6 per-body gate. After accepted A1 and A2 bodies exist, C4 setup → C4a–C4f in isolated parallel workspaces → C4g blind review; do not wait for all remaining skills. |
| 3 — then | B3 ∥ B4 ∥ B5; A3 followed by A4a–A4h, at most two skill rewrites at a time; A5 can run independently. B6 after its heading decision. D1–D3 only after the other session's tree is clear and authority exists. |
| Final gates | C4h proves the GLM rewritten-skill page; B3 final acceptance includes integrated B4/B5 outcomes; A6 reconciles all eleven visual replacements. Never label comparison success, publication or owner acceptance from drafts alone. |

A6 is a rolling publication work unit, not a dependency that forces all eleven rewrites ahead of A2 or the comparison. A4a–A4h expand P's grouped A4; B1 and C4 are split into independently assigned operations. No wave below must finish before independent work in an earlier wave starts.

## Wave 0 — gate machinery

### C1 — Design-review capsule template
- **NOT READY:** template destination/schema and current role-first review body are absent; GLM visual gate conflicts with R.
- Route: `gpt-6-astra` — writing a bounded reviewer assignment from evidence fits its planning/review role.
- Inputs: common manifest; supplied by the parent: current capsule schema, design language, maker pre-flight, tile directory/index, release binding, and accepted A3 body (current body only for a clearly provisional draft).
- Outputs: `RESULT.md` containing the complete proposed template; final template file name/path must be supplied by the parent.
- Acceptance: fields explicitly name reviewer route, role-first `design-review`, ≤2000px tiles, language, pre-flight and findings format `page@width / what / evidence / rule / proposed fix`; both reviewers use the same revision.
- Review gate: `glm-5.3-flash` checks textual completeness and binding fields; `gpt-6-astra` rendered use remains gated on supported evidence delivery, not template validity alone.
- Dependencies: A3 for final use, not initial drafting; owner first: resolve the unsupported GLM visual-review assignment.

### C2 — Maker response artifact and disposition contract
- **NOT READY:** report paths, finding identifiers and proposed answer/report adjacency are not supplied.
- Route: `gpt-6-astra` — defining the written response contract is documentation, not visual making.
- Inputs: common manifest; supplied by the parent: existing `control/design-qa-20260910.md`, reviewer reports with stable findings, C1 template and exact report/answer locations.
- Outputs: `RESULT.md` defining the response contract; `deliverable.patch` only for the supplied `control/design-qa-20260910.md` target, preserving its existing evidence.
- Acceptance: one maker-authored applied or overruled-with-reason disposition per finding, answer beside report, explicit revision/evidence references; the template writer must not fabricate maker answers.
- Review gate: `glm-5.3-flash` reconciles finding IDs against the written contract and existing first example; each actual maker owns its later answers.
- Dependencies: C1 draft; owner first: none beyond any gate resolution in C1.

### C3 — Extend and prove the rendered checker
- **NOT READY:** checker checkout, article scope, balanced-last-line metric/threshold and headline selectors are missing.
- Route: `glm-5.3-flash` — implementation is suitable once selectors and pass/fail rules are settled.
- Inputs: common manifest; supplied by the parent: `build/check-render.js`, `build/check.py`, `build/check.sh`, relevant CSS/JS/render inputs, local dependency set, fixtures and existing wheel-probe evidence.
- Outputs: `deliverable.patch`; `RESULT.md` with changed checks and parent execution evidence still required.
- Acceptance: ≤3 headline lines at 390/1440; duplicate article-heading checks with B6 scope decision respected; vertical wheel crosses `.ideas-row`, `.track-row`, `.table-scroll`, `.reader pre`; rail containment retained; balanced-last-line test uses an agreed metric; `check.sh` preserves structure/links/hashes/overflow/scrollbar/reveal/sentence checks.
- Review gate: `gpt-6-astra` reviews diff and parent pass/fail probes: forbidden examples fail, permitted pages pass; preserve already-landed wheel probe instead of duplicating it. Real Safari/Firefox trackpad behavior remains separately unverified without actual tests.
- Dependencies: B6 only for expansion beyond idea-page heading scope; owner first: heading-template decision; parent must settle the missing measurable balance rule.

## Wave 1 — first skill and handbook work already underway

### A1 — Rewrite `ui-ux-design`
- **NOT READY:** current body/references, full gpt-taste source, target path and authoring mechanism inputs are missing; S's strict no-repeat figure rule differs from its worked example's maximum-two rule.
- Route: `claude-fable-5-1` — P assigns Fable the creative-method opener and design defaults.
- Inputs: common manifest; supplied by the parent: exact current `ui-ux-design` body and dependencies, full gpt-taste `SKILL.md`, approved opener voice and figure-repetition rule.
- Outputs: `deliverable.patch` targeting the supplied skill body; `RESULT.md` with preservation comparison and authoring evidence.
- Acceptance: role first; named defaults, variance/menu/bans; `<design_plan>` covers band figures/focal points, line counts, one accent, meaningful motion, ban sweep and no instruction copied into output; preserve interview, routes, verification and provenance below opener; purpose unchanged.
- Review gate: `gpt-6-astra` compares exact revised body to S/gpt-taste and original obligations before A6 publication; no publication from a review verdict alone.
- Dependencies: none; owner first: approve worked-opener voice before replication and resolve figure-rule ambiguity.

### B1a — Reconcile the delivered idea-page writing
- **NOT READY:** authored copy and grounding sources are absent; exact copy-layer file names are not supplied.
- Route: `gpt-6-astra` — grounded prose and reconciliation are its evidenced writing use.
- Inputs: common manifest; supplied by the parent: bb4735a-equivalent current idea sources, `video-tips.json`, `video-research.md`, authored-layer schema, previous review and all 19 page targets.
- Outputs: `RESULT.md` mapping all 76 sections to source evidence; `deliverable.patch` only if missing/incorrect delivered prose needs repair, with exact targets supplied first.
- Acceptance: reconcile rather than rewrite accepted work; 19 pages/76 sections, subject-specific nonrepeated headings, ≥3 grounded sentences per prose section, no schema-label headings, invented claims or erased caveats/attribution.
- Review gate: `glm-5.3-flash` performs B1b claim checking from text; `gpt-6-astra` independently reviews rendered copy after integration using parent tiles in B1d.
- Dependencies: none; owner first: none.

### B1b — Check every idea section against evidence
- **NOT READY:** B1a revision, grounding inputs and actual prior second-route review are not supplied.
- Route: `glm-5.3-flash` — claim-by-claim reconciliation over text is suitable; no images are needed.
- Inputs: common manifest; supplied by the parent: B1a's 76 sections, `video-tips.json`, `video-research.md`, prior review and SHA256 for every readable input.
- Outputs: `RESULT.md` with a verdict for every section and source-grounded findings.
- Acceptance: all 76 sections accounted for; every challenged claim cites section and evidence; preserve quoted-via-Theo/sponsor/opinion qualifications; explicitly identify unresolved evidence instead of inventing it.
- Review gate: `gpt-6-astra` checks coverage and source references; Fable answers findings during B1c/B1d.
- Dependencies: B1a; runs parallel with B1c; owner first: none.

### B1c — Integrate the accepted idea-page copy
- **NOT READY:** handbook checkout, authored-layer paths and own-HEAD base are missing.
- Route: `claude-fable-5-1` — P explicitly assigns Fable integration alongside GLM's grounding review.
- Inputs: common manifest; supplied by the parent: B1a outputs, current renderer/authored layer, `build/check.py`, `build/check.sh`, handbook source tree and resolvable base; B1b findings when returned.
- Outputs: `deliverable.patch`; `RESULT.md` with integration result, check output and maker dispositions.
- Acceptance: preserve already-integrated bb4735a work; incorporate only necessary repairs, retain refusal of schema fallback, make `check.py` green, and do not land before B1b findings are answered and B1d passes.
- Review gate: `glm-5.3-flash` B1b grounding review parallel with integration; `gpt-6-astra` checks integrated rendered copy in B1d from parent tiles.
- Dependencies: B1a; B1b before landing, not before integration starts; owner first: none.

### B1d — Close idea-page checks and two-page owner read
- **NOT READY:** exact two page names, rendered tiles, integrated check logs and owner readback are missing; two-route visual gate remains unresolved.
- Route: `gpt-6-astra` — concise verification/reporting from supplied evidence fits this route.
- Inputs: common manifest; supplied by the parent: B1b/B1c outputs, `check.py`/`check.sh` logs, 390/1440 page tiles, exact two pages selected for owner reading and actual owner feedback.
- Outputs: `RESULT.md` recording observed checks, coverage, remaining findings and owner acceptance separately.
- Acceptance: grounding review covers every section; `check.py` green; two named pages rendered and actually read by the owner; all maker dispositions recorded; no owner or visual-gate pass inferred from build success.
- Review gate: `gpt-6-astra` rendered copy review plus `glm-5.3-flash` text grounding review; unsupported second visual verdict remains blocked, not relabeled as complete.
- Dependencies: B1b, B1c, C1, C2; owner first: select/read the two pages (selection may be supplied by parent, readback cannot).

### B2 — Finish investigation drawings and rendered review
- **NOT READY:** current `build/icons.py`, investigation evidence, five Boris layer names and 640px tiles are absent; final commit is not authorized here.
- Route: `claude-fable-5-1` — drawings of findings and visual refinement are its maker role.
- Inputs: common manifest; supplied by the parent: current `build/icons.py`/`INVESTIGATIONS`, three report sources, design language, pending redraw diff, pre-flight and parent 640px renders.
- Outputs: `deliverable.patch` only for needed redraw corrections; `RESULT.md` with pre-flight and maker answers.
- Acceptance: T3/Melee shows main line/fork/`13 PRs`/`VERIFIER ✓`; Course Video Manager shows `GLOSSARY`/`ONE TRANSPORT`/`RESUMABLE`; Boris shows staged compiler and five source-named validation layers; mono captions inside art; no invented layer names.
- Review gate: `gpt-6-astra` reads 640px drawings without adjacent explanatory prose and states what each shows before any commit; `glm-5.3-flash` checks caption/source facts only; global second visual gate unresolved.
- Dependencies: C1/C2 for final gate; owner first: none for corrections; any commit needs explicit authorization outside this ticket-writing run.

## Wave 2 — language and controlled comparison

### A2 — Rewrite `premium-tech-design-language`
- **NOT READY:** current body, vocabulary, six references, full gpt-taste source, target path and doctrine decisions are missing.
- Route: `claude-fable-5-1` — preserving measured design facts while making a creative menu needs design judgment.
- Inputs: common manifest; supplied by the parent: complete current language/vocabulary/six references, gpt-taste `SKILL.md`, accepted A1 and five approved doctrine resolutions.
- Outputs: `deliverable.patch`; `RESULT.md` mapping preserved values/motifs and explicit doctrine changes.
- Acceptance: role/defaults first; motifs become pre-flight choices; preserve all measured values, motifs and existing method except explicitly approved doctrine changes; resolve data-only numbered markers, meaning not mandatory library, spacing principle not fixed values, no font blacklist, no stock photography; unchanged purpose.
- Review gate: `gpt-6-astra` compares original facts/references and S/gpt-taste against exact new body before A6 per-body publication gate.
- Dependencies: A1 accepted; owner first: confirm/change all five doctrine recommendations; raster reversal is already requested, recorded when B4 lands.

### C4 — Freeze the six-way experiment
- **NOT READY:** new-page brief/name, current and rewritten skill snapshots, output page paths and blind scoring/tie rule are absent; GLM visual-making assignment conflicts with R.
- Route: `gpt-6-astra` — this unit specifies a controlled comparison, not its eventual blind judgment.
- Inputs: common manifest; supplied by the parent: one new handbook page brief, factual content/assets, frozen current/no-skill/rewritten conditions, A1/A2 accepted bodies, design-language rubric and exact six isolated workspace/page targets.
- Outputs: `RESULT.md` with identical brief, conditions, blind ID handling, render widths and acceptance rubric; no invented page title or source facts.
- Acceptance: same brief/assets/base for six builds; no-skill gets no design skills, current gets frozen originals, rewritten gets frozen accepted bodies; parent retains concealed condition mapping; blind reviewer receives no route/condition labels; define “beats” before builds.
- Review gate: `glm-5.3-flash` checks textual experiment controls; blind C4g must be a fresh review context without assignment/key leakage.
- Dependencies: A1, A2 bodies exist (not all eleven publications); owner first: resolve GLM making-design conflict without silently substituting a different experiment.

### C4a — GLM comparison: no design skill
- **NOT READY:** C4 brief/target paths and GLM visual-making conflict unresolved; cannot supply Fable's design without changing the experiment.
- Route: `glm-5.3-flash` — P requests this implementation arm, but R permits it only with settled nonvisual acceptance.
- Inputs: common manifest for parent only; supplied by the parent to this worker: C4 identical brief/content/assets/base, no design skills or design-standard contamination, declared input hashes.
- Outputs: `deliverable.patch`; `RESULT.md`; generated page file name supplied by parent in C4.
- Acceptance: implement exactly the frozen no-skill condition; no cross-arm artifacts; parent builds/renders the named page at C4 widths and retains failures as results.
- Review gate: fresh `gpt-6-astra` blind C4g review from anonymized tiles, never GLM image review.
- Dependencies: C4; owner first: C4 route-conflict resolution.

### C4b — GLM comparison: current design skills
- **NOT READY:** C4 paths and frozen current-skill list missing; GLM visual-making conflict unresolved.
- Route: `glm-5.3-flash` — this is the requested current-skill implementation arm, conditional on resolving its visual remit.
- Inputs: common manifest for parent only; supplied by the parent to this worker: identical C4 brief/content/assets/base, exact frozen current bodies and hashes, no rewritten bodies or S.
- Outputs: `deliverable.patch`; `RESULT.md`; generated page file name supplied by parent in C4.
- Acceptance: use only the current design-skill condition; isolated output; parent renders at identical C4 widths; record actual delivery/failure without changing conditions.
- Review gate: fresh `gpt-6-astra` blind C4g review of anonymized parent tiles.
- Dependencies: C4; owner first: C4 route-conflict resolution.

### C4c — GLM comparison: rewritten design skills
- **NOT READY:** C4 paths and exact rewritten skill selection missing; GLM visual-making conflict unresolved.
- Route: `glm-5.3-flash` — P requires this implementation arm to test whether the rewritten skills stand alone.
- Inputs: common manifest for parent only; supplied by the parent to this worker: identical C4 brief/content/assets/base, frozen rewritten A1/A2 selection and hashes, no maker coaching or other arms.
- Outputs: `deliverable.patch`; `RESULT.md`; generated page file name supplied by parent in C4.
- Acceptance: page built from the selected rewritten skills alone under identical conditions; parent captures C4 renders; retain artifact for C4h two-route proof.
- Review gate: fresh `gpt-6-astra` blind C4g review; C4h is distinct from comparative scoring.
- Dependencies: C4; owner first: C4 route-conflict resolution.

### C4d — Fable comparison: no design skill
- **NOT READY:** C4 brief, target names/paths and independent workspace are missing.
- Route: `claude-fable-5-1` — this is the design-maker arm without design-skill instruction.
- Inputs: common manifest for parent only; supplied by the parent to this worker: identical C4 brief/content/assets/base, no design skills, S or experiment condition key beyond its own assignment.
- Outputs: `deliverable.patch`; `RESULT.md`; generated page file name supplied by parent in C4.
- Acceptance: no-skill condition isolated from A1/A2 authoring context; same content and render protocol as five other builds; record failure rather than opportunistically adding a skill.
- Review gate: fresh `gpt-6-astra` blind C4g review from parent tiles.
- Dependencies: C4; owner first: none beyond C4 decisions.

### C4e — Fable comparison: current design skills
- **NOT READY:** C4 targets and exact frozen current skill selection are missing.
- Route: `claude-fable-5-1` — Fable makes the current-skill visual baseline requested by P.
- Inputs: common manifest for parent only; supplied by the parent to this worker: identical C4 brief/content/assets/base and frozen current skill bodies, excluding rewritten bodies and S.
- Outputs: `deliverable.patch`; `RESULT.md`; generated page file name supplied by parent in C4.
- Acceptance: fresh isolated context uses only the frozen current condition; no output sharing; parent renders at identical C4 widths.
- Review gate: fresh `gpt-6-astra` blind C4g review from anonymized parent tiles.
- Dependencies: C4; owner first: none beyond C4 decisions.

### C4f — Fable comparison: rewritten design skills
- **NOT READY:** C4 targets and exact frozen rewritten selection are missing.
- Route: `claude-fable-5-1` — Fable makes the rewritten-skill visual arm requested by P.
- Inputs: common manifest for parent only; supplied by the parent to this worker: identical C4 brief/content/assets/base and frozen rewritten A1/A2 selection, without other arms or reviewer results.
- Outputs: `deliverable.patch`; `RESULT.md`; generated page file name supplied by parent in C4.
- Acceptance: fresh isolated context implements frozen rewritten condition; same page content/widths; no retrospective tuning after seeing competing arms.
- Review gate: fresh `gpt-6-astra` blind C4g review from anonymized parent tiles.
- Dependencies: C4; owner first: none beyond C4 decisions.

### C4g — Blindly judge all six renders
- **NOT READY:** complete anonymous renders, coverage index and frozen scoring/tie rule are missing.
- Route: `gpt-6-astra` — evidence-based visual judgment is its designated comparison-review role.
- Inputs: common manifest for parent only; supplied by the parent to a fresh reviewer: six blind-ID render sets ≤2000px/axis, identical brief, approved language and frozen rubric, without producer/condition key or this ticket map.
- Outputs: `RESULT.md` with blind scores/findings and ranking; parent appends condition reconciliation only after the verdict is fixed.
- Acceptance: judge all six against one rubric; record unreadable/missing tiles; compare rewritten vs no-skill/current separately for each maker and under the predeclared overall rule; ties/losses do not become “rewritten wins.”
- Review gate: `glm-5.3-flash` audits score arithmetic/coverage from text after verdict, not images or visual judgment; parent verifies blind mapping and discloses any contamination.
- Dependencies: C4a, C4b, C4c, C4d, C4e, C4f; owner first: none after C4 rubric is settled.

### C4h — Prove the rewritten-skill GLM page meets the system bar
- **NOT READY:** C4c delivery, two-route visual-review resolution, severity rubric and owner feedback are missing.
- Route: `gpt-6-astra` — acceptance reconciliation from rendered and written evidence is review work.
- Inputs: common manifest; supplied by the parent: C4c page/source, pre-flight, independent reviewer reports, parent renders/check logs and actual owner willingness-to-share feedback.
- Outputs: `RESULT.md` with P §5 acceptance ledger, distinguishing comparative score, absolute page acceptance and owner response.
- Acceptance: a new page built by GLM from rewritten skills alone passes two-route review with no high finding and no “looks like every AI site” verdict; `check.sh` green; C4g actually demonstrates rewritten wins or explicitly records failure; owner willingness to send the link to Theo, Matt Pocock and Boris Cherny is recorded, not inferred and not a request to send it.
- Review gate: `gpt-6-astra` independent rendered review; proposed `glm-5.3-flash` second visual verdict is unsupported and cannot self-review its own C4c build; parent must resolve the independent second-route requirement.
- Dependencies: C4c, C4g, C1, C2, C3; owner first: review-gate resolution and eventual acceptance/readback.

## Wave 3 — handbook composition and assets

### B3 — Home composition under rewritten skills
- **NOT READY:** handbook source/assets, exact target paths, hero-copy decision, reviewed skill delivery and two-route visual gate are unresolved.
- Route: `claude-fable-5-1` — composition, type and focal-point decisions are design making.
- Inputs: common manifest; supplied by the parent: `build/home.json`, `build/render.py`, `build/assets/handbook.css`, `build/assets/handbook.js`, icons/assets, `DESIGN.md`, current home renders, accepted A1/A2, B1/B2 results and final B4/B5 outcomes.
- Outputs: `deliverable.patch`; `RESULT.md` containing pre-flight, render references, every maker disposition and owner QA result.
- Acceptance: pre-flight names distinct band figures/focal points/headline line counts before build; no uniform-grid/empty-cell/postage-stamp defaults, instruction headings or block-text closing; retain quiet frames/no homepage stills unless reversed; ≤3 headline lines at 390/1440; `check.sh` green; no high/AI-site verdict; owner QA finds none of today's eleven-note defects.
- Review gate: `gpt-6-astra` copy/render review ∥ `glm-5.3-flash` text/source/measurement checks, then Fable answers every finding, then owner; GLM composition verdict remains blocked by R, so this is not yet the full required visual gate.
- Dependencies: A1, A2, B1d, B2, C1, C2, C3; B4/B5 before final integrated acceptance, not before initial composition work; owner first: choose hero headline, resolve gate; frames remain settled unless changed.

### B4 — Higgsfield problem-panel graphic and seam strips
- **NOT READY:** generation requires network/credits/credentials outside this boundary; asset names, seam count, source paths and placement decision are missing. Do not substitute a different tool/model.
- Route: `claude-fable-5-1` — prompt direction, asset selection and integration are visual design.
- Inputs: common manifest; supplied by the parent: authorized Higgsfield execution context or resulting raw assets, verbatim `higgsfield-generate`, approved language, panel source/CSS, `DESIGN.md`, `control/design-notes.md`, and exact asset targets under `public/assets/`.
- Outputs: `deliverable.patch`; `RESULT.md`; raw and shipped image file names must be declared by parent before generation (none invented here).
- Acceptance: GPT Image 2.5 / `gpt_image_2_5`, three prompts × two panel variants (`flare`/`sunburst`), 2k/3:4; seams 21:9 with count supplied; no text/faces/product UI; measure PIL nonopaque-pixel percentage, luminance-key opaque files and verify composite on `#0c0c0d`; keep raws; shipped asset <150 KB, local-only references.
- Acceptance continued: retain eight rows/eleven links and `n_pick == 8`; selected placement behind rows at 0.18–0.28 opacity or third column ≥1200px; replace seams only if cleanly keyed; record prompts/model/params/actual credits and raster-rule reversal in supplied notes/`DESIGN.md` when asset lands.
- Review gate: `gpt-6-astra` reviews parent tiles at 390/1440 and 1200px threshold if third-column; `glm-5.3-flash` checks textual alpha/bytes/link evidence, never images; maker answers; full second visual gate unresolved.
- Dependencies: A2 and C1/C2 for landing; coordinate B3 shared CSS/home writes, not serial generation; D1 only if governed skill delivery is actually unavailable; owner first: choose placement after first set renders, plus separate authority for external generation.

### B5 — Decide and, only if earned, build one pinned study
- **NOT READY:** investigation source, motion evidence, exact patch targets and criterion for “earns 44 KB” are missing.
- Route: `claude-fable-5-1` — deciding whether pinning improves meaning is visual/interaction judgment.
- Inputs: common manifest; supplied by the parent: accepted A1/A2, investigations source/diagrams, existing typed prompt/trace/reveals/seams, native-motion prototype, parent motion evidence and local GSAP/ScrollTrigger assets only if authorized and selected.
- Outputs: `RESULT.md` documenting native-vs-pinned decision; `deliverable.patch` only if the approved one-study implementation is justified, with library file names supplied first.
- Acceptance: retain working existing motion; first assess native implementation; either document no GSAP, or one pinned/scrubbed study on `investigations.html`; measure actual added weight against historical 44 KB, vendor locally, reduced-motion finished state, no-JS controls, no scroll-jail, `min-height: 100svh` not fixed `100vh`.
- Review gate: `gpt-6-astra` reviews parent motion captures/measurements and rendered states; `glm-5.3-flash` checks source/fallback/weight text; maker answers; Safari/Firefox/real-device behavior requires actual parent tests, not still-image inference.
- Dependencies: A1, A2, B2, C1/C2 for landing; runs parallel with B3/B4 before shared-file integration; owner first: resolve value criterion if native/pinned choice remains consequential and unsettled.

### B6 — Settle repeated guide/skill headings and apply the decision
- **NOT READY:** owner choice, exact guide/skill files, five skill-section names and exemption/rewrite scope are missing.
- Route: `gpt-6-astra` — the central work is a documented editorial decision and grounded heading writing.
- Inputs: common manifest; supplied by the parent: guide/skill sources, exact recurring headings, `ARCHITECT.md`, checker scope and owner choice to keep templates or rewrite.
- Outputs: `RESULT.md` specifying chosen scope and heading mapping; `deliverable.patch` for named content/rule targets only, leaving checker implementation to C3.
- Acceptance: account for guides' `When to apply / Implementation / Acceptance / Failure modes and maintenance` and all five actual skill headings; if retained, explicit template exception; if rewritten, grounded subject-specific headings and updated links; keep checker restricted to ideas until decision is made.
- Review gate: `glm-5.3-flash` checks text coverage, unique headings and link mapping; `gpt-6-astra` independently reviews parent rendered changes if any, followed by maker answers and the unresolved full visual gate.
- Dependencies: none for owner decision; C3 consumes the decision; owner first: keep shared document-template headings or rewrite them.

## Wave 3 — remaining skill replacements (at most two concurrently)

For A3 and A4a–A4h, “standard acceptance” means S's role → defaults → forced variance → bans → menus → pre-flight before the preserved existing method; add the no-instruction-as-output line, preserve evidence/provenance and current obligations, and keep frontmatter purposes unchanged. This is a comparison against complete supplied originals, not permission to discard them. Parent must resolve S's figure-repetition discrepancy through A1. Reviews use full gpt-taste, not only S's quoted opener. Each exact body passes its own A6 publication gate; no batch verdict substitutes for eleven reviews.

### A3 — Rewrite `design-review`
- **NOT READY:** current full body/references, target path and full gpt-taste are missing.
- Route: `claude-fable-5-1` — P assigns Fable the creative-method rewrites, including how reviewers recognize defaults.
- Inputs: common manifest; supplied by the parent: complete `design-review` source/dependencies, gpt-taste, accepted A1/A2 and approved voice/doctrine decisions.
- Outputs: `deliverable.patch`; `RESULT.md` with standard/preservation comparison and authoring evidence.
- Acceptance: standard acceptance; findings explicitly name the default the maker fell into, not just a token deviation; preserve rendered-evidence requirements and distinguish unseen work.
- Review gate: `gpt-6-astra` compares old/new body against S/gpt-taste and evidence obligations before A6 publication.
- Dependencies: A1, A2; owner first: approved voice/doctrines from A1/A2.

### A4a — Rewrite `design-implementation`
- **NOT READY:** complete original/dependencies, target path and authoring inputs are missing.
- Route: `claude-fable-5-1` — this is creative-method authorship, not bulk UI implementation.
- Inputs: common manifest; supplied by the parent: full `design-implementation` source/dependencies, gpt-taste and accepted A1/A2/A3.
- Outputs: `deliverable.patch`; `RESULT.md` with preservation comparison and authoring evidence.
- Acceptance: standard acceptance adapted to implementation defaults; retain existing implementation and rendered-verification obligations, exact facts and unchanged purpose.
- Review gate: `gpt-6-astra` body-by-body comparison to original, S and gpt-taste before A6.
- Dependencies: A3; owner first: none beyond A1/A2 decisions.

### A4b — Rewrite `interaction-design`
- **NOT READY:** complete original/dependencies, target path and authoring inputs are missing.
- Route: `claude-fable-5-1` — interaction defaults and meaningful motion need design-method judgment.
- Inputs: common manifest; supplied by the parent: full `interaction-design` source/dependencies, gpt-taste and accepted A1/A2/A3.
- Outputs: `deliverable.patch`; `RESULT.md` with preservation comparison and authoring evidence.
- Acceptance: standard acceptance adapted to interaction failures; preserve all existing interaction/accessibility/verification duties actually present in the source; adopt only approved motion doctrine; unchanged purpose.
- Review gate: `gpt-6-astra` compares exact body with original, S and gpt-taste before A6.
- Dependencies: A3; owner first: none beyond A1/A2 decisions.

### A4c — Rewrite `design-system`
- **NOT READY:** complete original/dependencies, target path and authoring inputs are missing.
- Route: `claude-fable-5-1` — system-level visual choices and anti-default menus are creative-method work.
- Inputs: common manifest; supplied by the parent: full `design-system` source/dependencies, gpt-taste and accepted A1/A2/A3.
- Outputs: `deliverable.patch`; `RESULT.md` with preservation comparison and authoring evidence.
- Acceptance: standard acceptance specialized to system design; retain measured token facts and all original methods rather than replacing project values with gpt-taste's classes; unchanged purpose.
- Review gate: `gpt-6-astra` compares original values/method with revised body, S and gpt-taste before A6.
- Dependencies: A3; owner first: none beyond A1/A2 decisions; schedule after the first pair to maintain two-at-a-time limit.

### A4d — Rewrite `design-exploration`
- **NOT READY:** complete original/dependencies, target path and authoring inputs are missing.
- Route: `claude-fable-5-1` — compelling, genuinely varied exploration is creative-method work.
- Inputs: common manifest; supplied by the parent: full `design-exploration` source/dependencies, gpt-taste and accepted A1/A2/A3.
- Outputs: `deliverable.patch`; `RESULT.md` with preservation comparison and authoring evidence.
- Acceptance: standard acceptance specialized to exploration defaults; pre-flight commits to concrete alternatives rather than first-option repetition; preserve existing intent/routes/evidence; unchanged purpose.
- Review gate: `gpt-6-astra` compares original, S and gpt-taste against exact new body before A6.
- Dependencies: A3; owner first: none beyond A1/A2 decisions; schedule in the second pair.

### A4e — Rewrite `conversion-layout`
- **NOT READY:** complete original/dependencies, target path and authoring inputs are missing.
- Route: `claude-fable-5-1` — visual conversion composition is design making even though factual claims stay grounded.
- Inputs: common manifest; supplied by the parent: full `conversion-layout` source/dependencies, gpt-taste and accepted A1/A2/A3.
- Outputs: `deliverable.patch`; `RESULT.md` with preservation comparison and authoring evidence.
- Acceptance: standard acceptance with conversion-layout defaults/menus; preserve original conversion constraints and evidence qualifications; do not turn example layouts into mandatory recipes; unchanged purpose.
- Review gate: `gpt-6-astra` compares original obligations, S and gpt-taste against exact revised body before A6.
- Dependencies: A3; owner first: none beyond A1/A2 decisions; schedule in the third pair.

### A4f — Rewrite `explainer`
- **NOT READY:** complete original/dependencies, target path and authoring inputs are missing.
- Route: `claude-fable-5-1` — P treats this visual-explanation method as a creative rewrite, not a bulk prose ticket.
- Inputs: common manifest; supplied by the parent: full `explainer` source/dependencies, gpt-taste and accepted A1/A2/A3.
- Outputs: `deliverable.patch`; `RESULT.md` with preservation comparison and authoring evidence.
- Acceptance: standard acceptance adapted to explanation composition; preserve factual grounding, source qualifications and existing method; prohibit instruction labels masquerading as output; unchanged purpose.
- Review gate: `gpt-6-astra` compares original, S and gpt-taste against exact revised body before A6.
- Dependencies: A3; owner first: none beyond A1/A2 decisions; schedule in the third pair.

### A4g — Rewrite `prototype`
- **NOT READY:** complete original/dependencies, target path and authoring inputs are missing.
- Route: `claude-fable-5-1` — strengthening prototype design judgment is creative-method authorship.
- Inputs: common manifest; supplied by the parent: full `prototype` source/dependencies, gpt-taste and accepted A1/A2/A3.
- Outputs: `deliverable.patch`; `RESULT.md` with preservation comparison and authoring evidence.
- Acceptance: standard acceptance; preserve the existing role language where useful (S reports this is already the one “You are” skill), retain original prototype boundaries/method, add missing defaults/variance without losing obligations; unchanged purpose.
- Review gate: `gpt-6-astra` compares original, S and gpt-taste against exact revised body before A6.
- Dependencies: A3; owner first: none beyond A1/A2 decisions; schedule in the fourth pair.

### A4h — Rewrite `design-language-transfer`
- **NOT READY:** complete original/dependencies, target path and authoring inputs are missing.
- Route: `claude-fable-5-1` — transferring visual language without flattening it requires design judgment.
- Inputs: common manifest; supplied by the parent: full `design-language-transfer` source/dependencies, gpt-taste and accepted A1/A2/A3.
- Outputs: `deliverable.patch`; `RESULT.md` with preservation comparison and authoring evidence.
- Acceptance: standard acceptance specialized to transfer defaults; retain source measurements, provenance, qualification and existing transfer/verification method; unchanged purpose; `codebase-design` stays untouched.
- Review gate: `gpt-6-astra` compares original, S and gpt-taste against exact revised body before A6.
- Dependencies: A3; owner first: none beyond A1/A2 decisions; schedule in the fourth pair.

### A5 — Amend `precise` register scope
- **NOT READY:** editable full original/target and authorized authoring inputs missing; P asks one sentence while S proposes a two-sentence replacement.
- Route: `gpt-6-astra` — a narrowly scoped factual instruction change is writing rather than visual making.
- Inputs: common manifest; supplied by the parent: exact current `precise` source/dependencies, authorized target and approved resolution of P's one-sentence requirement versus S's quoted wording.
- Outputs: `deliverable.patch`; `RESULT.md` with scope-only wording comparison and loading evidence still required.
- Acceptance: one-sentence amendment if P is retained; facts/reports remain precise, creative methods own their register and open role/defaults-first; retain `simple` for people and all unrelated duties; do not claim active installation from an edit.
- Review gate: `glm-5.3-flash` checks scope/preservation against original and P/S; actual loading/publication uses an authorized mechanism, not direct release edits.
- Dependencies: none; owner first: resolve sentence-count/text discrepancy and whether this additional nonvisual amendment has separate publication scope.

### A6 — Authoring evidence and eleven replacement publications
- **NOT READY:** `skill-publish` interface, source/release targets, exact-byte evidence and publication authority are absent; publication is prohibited in this run.
- Route: `glm-5.3-flash` — mechanical packaging/reconciliation fits settled-interface implementation once the mechanism is supplied.
- Inputs: common manifest; supplied by the parent: authorized authoring/publication instructions, exact eleven bodies with SHA256/evidence, Astra per-body reviews, previous versions/recovery procedure and live loading evidence.
- Outputs: `RESULT.md` recording per-body draft/review/authoring/publication/selection/delivery states and actual receipts; any mechanism-required output file names must be supplied before execution.
- Acceptance: eleven replacements in place: A1/A2/A3 plus eight A4 bodies; publisher core, purposes unchanged, no added skill/cap relabeling; evidence bound to exact bytes; review each against gpt-taste before publication; preserve previous versions and prove actual selected body delivery if activation is claimed.
- Review gate: `gpt-6-astra` reviews each body before its publication and reconciles evidence/receipts after; no new purpose-review stage invented, and publication alone does not prove worker delivery.
- Dependencies: each individual A1/A2/A3/A4 body and its review before that body's operation; all eleven before final reconciliation; owner first: authorize publication separately and resolve A5's additional amendment (not counted as one of the eleven visual bodies).

## Wave 3 — runtime lane, only when the other session's tree is clear

### D1 — Apply, prepare, publish and switch product-skills runtime
- **NOT READY:** live runtime mutation/publication/switch exceeds this boundary; source/README/patch/tests/preparer and a clear-tree handoff are absent.
- Route: `glm-5.3-flash` — P describes implementation with a supplied patch and fixed procedure, not unresolved architecture.
- Inputs: common manifest; supplied by the parent: full `control/nautilus-product-skills/` including `README.md`, patch/tests/preparer, exact owned runtime checkout/base, authorized procedure, approved target release and recovery path preserving other edits.
- Outputs: `RESULT.md` with each observed apply/preparer/publish/switch state and evidence; `deliverable.patch` only for authorized local staging; generated runtime/publication file names await the README.
- Acceptance: follow supplied README steps without inventing commands; preserve eight manufacturer skill bodies verbatim; relevant tests pass; publication, switch and actual governed skill delivery are separately evidenced; do not treat installed CLI or staged patch as delivered runtime support.
- Review gate: `gpt-6-astra` reviews patch/tests, actual preparer/publish/switch records and worker-delivery evidence; executable enforcement claims require permitted/blocked behavioral probes, not a configuration-only pass.
- Dependencies: no A/B ticket; other session's tree clear before mutation; owner first: explicit go and authorized runtime/publication scope unavailable in this run.

### D2 — Readopt `copy` whole
- **NOT READY:** complete staged `copy`, provenance, authoring-run specification, mechanism output names and publication authority are absent.
- Route: `gpt-6-astra` — this is writing-method authoring evidence and whole-source reconciliation, not visual making.
- Inputs: common manifest; supplied by the parent: complete staged `copy` body/companions/provenance, authoring brief/evidence requirements, existing domain classification record and authorized `skill-publish` procedure/target.
- Outputs: `RESULT.md` with whole-readoption comparison and authoring evidence; publication-generated file names must come from the supplied mechanism, not this backlog.
- Acceptance: readopt whole, preserve original useful expertise and source qualifications; authoring evidence bound to exact bytes precedes `skill-publish`; record draft/admitted/published/delivered separately; do not count it among eleven visual core replacements.
- Review gate: `glm-5.3-flash` checks complete-source/provenance/evidence reconciliation from text; `gpt-6-astra` authoring result is not itself publication evidence.
- Dependencies: clear-tree handoff; D1 only if the supplied adoption mechanism requires its product-skills change, not assumed; owner first: explicit go for the authoring run and separate publication authorization.

### D3 — Decide repository treatment of agent installation files
- **NOT READY:** repository path/status, contents/provenance of `.agents/`, `.claude/`, `skills-lock.json`, ignore target and owner commit authorization are missing.
- Route: `glm-5.3-flash` — bounded inventory and approved ignore/staging changes suit mechanical implementation.
- Inputs: common manifest; supplied by the parent: exact repository and own-HEAD base, current edits/status, sanitized inventory of the three named paths, existing ignore files and project retention policy.
- Outputs: `RESULT.md` with per-path commit-or-ignore recommendation and privacy checks; `deliverable.patch` only for an approved, explicitly named ignore target; no commit performed by this backlog.
- Acceptance: account for all three paths; preserve other session's edits; inspect for credentials/private application state without printing secrets; commit only safe project files under explicit authorization, or ignore agreed paths without deleting content; final status demonstrates chosen treatment.
- Review gate: `gpt-6-astra` reviews sanitized inventory, rationale, patch and status evidence; a clean tree alone is not proof private data was excluded.
- Dependencies: other session's tree clear and ownership handoff; independent of D1/D2 absent a supplied technical prerequisite; owner first: approve consequential commit-vs-ignore treatment and any commit.

## Completion boundary

Coverage: P A1–A3, all eight A4 skill names, A5 amendment and A6 governance; B1 writing/review/integration/readback, B2–B6; C1–C3 and C4's setup, six builds, blind review and §5 proof; D1–D3 are represented. Eleven visual skill replacements remain eleven, with `precise` separately identified; `codebase-design` remains excluded. Historical notes are constraints/state evidence, not eleven extra repair tickets reopening completed work.

The ticket inventory is complete; execution readiness is not. Resolve exact worker/source/output bindings, owner decisions, the GLM visual-role contradiction, and unavailable effects before dispatching affected tickets. Do not remove required Charts, invent evidence, loosen checks, or substitute routes/tools to make a blocked ticket appear ready.
