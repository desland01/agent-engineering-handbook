# The method additions exceed the selected library allowance

September 10, 2026. Scope: the existing Precise, Create Skill and Long-running Harness
proposals; Pilot First is combined into the last of these, with no new skill.

GLM 5.3 Flash run `d4e74f36-c005-4ddb-80bf-eb569b2075c7` completed native authoring under
`handbook-heading-corrections`. Its workspace is `var/workspaces/glm-method-publication`.
The retained `out/RESULT.md` records the merge, and `out/attestation.json` seals all three
bodies and their unchanged purposes. The existing verify-run helper passed model identity,
plugin path and exact native delivery of Precise and Create Skill.

The parent inspected the full diff: exactly three bodies changed, with additive sections
and preserved existing frontmatter, invocation, purposes and body text. Two errors in the
harness draft were corrected: an accepted ticket proves the pilot, whereas worker delivery
alone does not; the installation-home prohibition belongs to the recorded ticket controller,
not every runtime entry. Ordinary native authoring uses its own declared workspace contract.

The proposal was copied outside installation home into `.scratch/method-publication`, as the
publication entry requires. `nautilus skill-publish` refused `skill_content_library_bytes`.
The same selected validator counts 2,096,342 active bundle bytes before this change and
2,107,405 in the proposal, against 2,097,152 allowed. The additions need 11,063 bytes;
only 810 remain, leaving a 10,253-byte conflict. The limit, validator and content were not
weakened or trimmed to obtain admission. No candidate was activated. The selected release
remains `handbook-heading-corrections`, whose prior release is retained for recovery.

All three merged bodies, native attestation and publication evidence remain in the authoring
workspace. The original proposals remain in `control/proposals/`. Resolving this policy/content
conflict is required before publication; this report does not assert that written additions
are loaded or effective.
